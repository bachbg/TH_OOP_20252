Question: If you create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Does JAVA allow you to do this?

Answer: Yes, it is called constructor overloading. Java allows defining multiple constructors with the same name as long as the parameter lists are different. The compiler will decide which to call based on the parameter provided when creating the object.